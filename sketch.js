let x = 0;
let y = 0;
let s = 20;

function setup() {
createCanvas(windowWidth, windowHeight);
  stroke(100);
  strokeWeight(1);
  fill(random(0,255), random(0,255), random(0,255) )
}

function draw() {
  background(0, 20);
  translate(width/2, height/2);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }
  
    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }
  
  
      translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }
  
      translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }
      translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }
      translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }
  
      translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }
      translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }
      translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }
      translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }
      translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }
      translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }
      translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }
      translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }
      translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }
      translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }
      translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }
      translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }

      translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }
  
      translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }    translate(width/100, height/100);
  rect(x*s, y*s, s, s);
  if(random() < 0.5 && x < width/2/s){ x++; }
  if(random() > 0.5 && x > -width/2/s){ x--; }
  if(random() < 0.5 && y < height/2/s){ y++; }
  if(random() > 0.5 && y > -height/2/s){ y--; }
  
}
